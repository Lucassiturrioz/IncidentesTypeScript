<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Sesión</title>
    <style>
        /* Estilos CSS */
        body {
            font-family: Arial, sans-serif;
            background-color: #2ecc71; /* Color de fondo verde */
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .login-container {
            width: 400px; /* Ancho del contenedor */
            height: 450px; /* Alto del contenedor */
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            border-radius: 5px;
            text-align: center;
        }

        h1 {
            color: #333;
        }

        label {
            display: block;
            margin-bottom: 10px;
            color: #333; /* Texto en color negro */
        }

        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 8px; /* Tamaño de relleno ajustado */
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            display: block; /* Para que los campos de entrada ocupen todo el ancho */
            margin: 0 auto; /* Centro horizontalmente */
        }

        button {
            background-color: #3498db; /* Botón azul */
            color: #fff;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #2980b9; /* Color azul más oscuro al pasar el cursor */
        }

        /* Estilo para el enlace "¿Olvidó su contraseña?" */
        .forgot-password {
            color: #888;
            text-decoration: underline;
            margin-top: 10px;
            display: block;
        }
    </style>
</head>
<body>
<div class="contenedor-inicio-sesion">
    <h1>Iniciar Sesión</h1>
    <form action="/login" method="post">
        <label for="correoElectronico">Nombre de Usuario:</label>
        <input type="text" id="correoElectronico" name="correoElectronico" required>
        <br>
        <label for="contrasenia">Contraseña:</label>
        <input type="password" id="contrasenia" name="contrasenia" required>
        <br>
        <button type="submit">Iniciar Sesión</button>
    </form>
    <a class="olvido-contraseña" href="/olvido-contraseña">¿Olvidó su contraseña?</a>
    <a class="Sos-nuevo-Registrate" href="/usuarios/crear">¿Sos nuevo? Registrate!</a>

</div>
</body>
</html>


